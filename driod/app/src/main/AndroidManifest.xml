<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.problemsolver">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.ProblemSolver">
        <activity android:name=".puzzleP" android:parentActivityName=".PUZZLEscreen"></activity>
        <activity android:name=".fwgcP" android:parentActivityName=".FWGCscreen"/>
        <activity
            android:name=".PUZZLEscreen"
            android:parentActivityName=".MainActivity" />
        <activity
            android:name=".FWGCscreen"
            android:parentActivityName=".MainActivity" />
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:theme="@style/Theme.ProblemSolver.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>